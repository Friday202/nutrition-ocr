# Where the raw data is stored, data for sorie other for mine
DATA_DIR = "../data_ocr"

# Where the processed dataset and processor are saved
PROCESSED_DATASET_DIR = "processed_dataset_ocr"
PROCESSOR_DIR = "processor_ocr"

# Where the final model is saved
MODEL_DIR = "local_model_ocr"

# Where to save outputs during training
OUTPUT_DIR = "outputs/donut-ocr"

TEST_SIZE = 0.1
VAL_SIZE = 0.1
SEED = 42
